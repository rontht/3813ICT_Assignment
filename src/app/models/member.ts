export class Member {
  username: string;
  name: string;
  role: string;

  constructor(username: string, name: string, role: string) {
    this.username = username;
    this.name = name;
    this.role = role;
  }
}
